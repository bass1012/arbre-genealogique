{
  "name": "arbre-genealogique-mysql",
  "version": "1.0.0",
  "description": "Application arbre généalogique pour LWS avec MySQL",
  "main": "server/dist/index.js",
  "scripts": {
    "start": "node server/dist/index.js",
    "postinstall": "cd server && npm install && npm run build && cd ../client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mysql2": "^3.11.0",
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express-validator": "^7.3.1",
    "multer": "^2.0.2",
    "cloudinary": "^2.8.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
